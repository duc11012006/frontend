// let one = 1, 
// two = -6, 
// three = 6;

// console.log(`Max ${one}, ${two}, ${three}: ${Math.max(one, two, three)}`);


let one   = 1;
let two   = 5;
let three = 19;

console.log(`Max ${one}, ${two}; ${three} : ${Math.max(one,two,three)}`);